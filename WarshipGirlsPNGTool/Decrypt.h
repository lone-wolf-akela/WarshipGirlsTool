#pragma once

#include <array> 
#include <string>
#include <vector>
#include <sstream>
#include "Tools.h"

/**
* 解密PNG图片
*/
std::stringstream DecryptMuka(std::string filename);
std::stringstream DecryptMukaR(std::string filename);